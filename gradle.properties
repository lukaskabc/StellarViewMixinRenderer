org.gradle.jvmargs=-Xmx1G
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true

parchment_minecraft_version=1.21.1
parchment_mappings_version=2024.11.17

# Environment Properties

minecraft_version=1.21.1
minecraft_version_range=[1.21.1]

# The Neo version must agree with the Minecraft version to get a valid artifact
neo_version=21.1.72
neo_version_range=[21.1.0,)

# The loader version range can only use the major version of FML as bounds
loader_version_range=[1,)

## Mod Properties

mod_id=stellarview_mixin_renderer
mod_name=StellarView Mixin renderer

mod_license=MIT

# replaced by GitHub Action with version from the release tag, see .github/workflows/publish.yml
mod_version=0.1-dev-neoforge
mod_group_id=cz.lukaskabc.minecraft.mod.stellarview.mixin_render
